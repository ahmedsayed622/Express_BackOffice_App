NODE_ENV=development
APP_PORT=3000
SERVER_HOST=0.0.0.0

API_KEY=
# Run: npm run gen:api-key to generate and write API_KEY into .env (do not commit .env)

DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=127.0.0.1
DB_PORT=1521
DB_NAME=your_service_name

DB_SYNC=false

ORACLE_CLIENT_PATH=
ORACLE_CLIENT_LIB_DIR=

DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

ORA_POOL_MIN=2
ORA_POOL_MAX=10
ORA_POOL_TIMEOUT=60
ORA_QUEUE_MAX=500
ORA_QUEUE_TIMEOUT=60000
ORA_STMT_CACHE=50

ALLOWED_ORIGINS=http://localhost:3000
ENABLE_RATE_LIMITING=false

LOG_LEVEL=info
LOG_FILE=logs/app.log

JWT_SECRET=change_me
JWT_EXPIRES_IN=24h
SESSION_SECRET=change_me
BCRYPT_ROUNDS=12
